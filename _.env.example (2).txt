# URL backend API utama (tanpa trailing slash)
VITE_API_URL=http://localhost:3000

# URL backend bot WhatsApp (tanpa trailing slash)
# Kalau bot jalan di server yang sama dengan API, gunakan nilai yang sama
VITE_BOT_API_URL=http://localhost:3001
